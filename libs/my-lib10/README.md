# my-lib10

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build my-lib10` to build the library.

## Running unit tests

Run `nx test my-lib10` to execute the unit tests via [Jest](https://jestjs.io).
