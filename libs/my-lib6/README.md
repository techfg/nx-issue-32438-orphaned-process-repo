# my-lib6

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build my-lib6` to build the library.

## Running unit tests

Run `nx test my-lib6` to execute the unit tests via [Jest](https://jestjs.io).
