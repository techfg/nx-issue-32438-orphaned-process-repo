# my-lib5

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build my-lib5` to build the library.

## Running unit tests

Run `nx test my-lib5` to execute the unit tests via [Jest](https://jestjs.io).
